"""Infrastructure layer: storage, GitHub, and configuration adapters.

Each module here implements a protocol declared in ``application.ports``. The
application layer never imports these modules directly except at composition
time.
"""
